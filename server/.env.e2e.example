# Dedicated disposable database used only by Playwright's real-stack tests.
# docker compose up -d db-e2e exposes it on localhost:5433 by default.
E2E_DATABASE_URL=postgresql://schedule_app:change-me-for-local-development@127.0.0.1:5433/schedule_assistant_e2e
